# use NetworkVisCreator as a baseline
# instead of appending to a graph dict, add as node attribute dict
# also add repo and component ID
# can use same structure of loading all components from one pickle file
# then upload all to local
